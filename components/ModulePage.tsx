'use client';import Link from 'next/link';import {useState} from 'react';export default function ModulePage({title,description,pro=false,children}:{title:string;description:string;pro?:boolean;children?:React.ReactNode}){const [open,setOpen]=useState(false);return <><div className="page-head"><div><h1>{title} {pro&&<span className="badge badge-pro">PRO</span>}</h1><p className="muted">{description}</p></div><Link href="/app" className="btn">Dashboard</Link></div>{children||<div className="card"><div className="empty"><h3>Ready when you are</h3><p className="muted">This workspace is wired to the Blogger-first architecture. Connect Blogger to load live data.</p><Link href="/app/blogs" className="btn btn-primary">Connect Blogger</Link></div></div>}{pro&&<button className="btn" style={{marginTop:12}} onClick={()=>setOpen(true)}>Why is this PRO?</button>}{open&&<div className="card" style={{marginTop:12}}><b>PRO feature</b><p className="muted">PRO focuses on automation, scale and convenience while keeping core writing and publishing useful on FREE.</p><Link href="/app/subscription" className="btn btn-primary">View subscription</Link></div>}</>}
