import {NextRequest,NextResponse} from 'next/server';import {getSession} from '@/lib/session';import {getSettings,saveSettings} from '@/lib/settings';
export async function GET(){if(!(await getSession()))return NextResponse.json({error:'Connect your Google account first.'},{status:401});return NextResponse.json(await getSettings())}
export async function POST(req:NextRequest){if(!(await getSession()))return NextResponse.json({error:'Connect your Google account first.'},{status:401});try{const x=await req.json(),allowed=['seoTitlePattern','defaultMetaDescription','defaultSchema','organizationName','authorName','socialImage','facebookAppId','twitterSite','canonicalMode'],patch:any={};for(const k of allowed)if(x[k]!==undefined)patch[k]=String(x[k]);return NextResponse.json(await saveSettings(patch));}catch{return NextResponse.json({error:'Could not save settings.'},{status:400})}}
