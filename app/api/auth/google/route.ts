import {NextResponse} from 'next/server';import crypto from 'node:crypto';import {oauth,BLOGGER_SCOPE,SEARCH_CONSOLE_SCOPE,ANALYTICS_SCOPE} from '@/lib/google';
export async function GET(){if(!process.env.GOOGLE_CLIENT_ID||!process.env.GOOGLE_CLIENT_SECRET)return NextResponse.json({error:'Google OAuth is not configured on the server.'},{status:500});const state=crypto.randomUUID();const res=NextResponse.redirect(oauth().generateAuthUrl({access_type:'offline',prompt:'consent',include_granted_scopes:true,scope:['openid','email','profile',BLOGGER_SCOPE,SEARCH_CONSOLE_SCOPE,ANALYTICS_SCOPE],state}));res.cookies.set('wyblog_oauth_state',state,{httpOnly:true,secure:process.env.NODE_ENV==='production',sameSite:'lax',path:'/',maxAge:600});return res}
