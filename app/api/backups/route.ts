import {NextResponse} from 'next/server';import {getSession} from '@/lib/session';import {buildBackup} from '@/lib/backup';
export async function GET(){const s=await getSession();if(!s)return NextResponse.json({error:'Connect your Google account first.'},{status:401});try{const pack=await buildBackup(s);return new NextResponse(JSON.stringify(pack,null,2),{headers:{'Content-Type':'application/json','Content-Disposition':`attachment; filename="wyblog-${new Date().toISOString().slice(0,10)}.wybak"`}});}catch{return NextResponse.json({error:'Could not create a backup.'},{status:500})}}
