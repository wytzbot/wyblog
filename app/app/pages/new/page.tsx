'use client';import {useState} from 'react';import {useRouter} from 'next/navigation';
export default function Page(){const[t,setT]=useState(''),[c,setC]=useState('<p>Write your page…</p>'),[busy,setBusy]=useState(false);const r=useRouter();async function save(){setBusy(true);const x=await fetch('/api/pages',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({title:t,content:c})}).then(v=>v.json());setBusy(false);if(!x.error)r.push('/app/pages');else alert(x.error)}return <><div className="page-head"><div><h1>New Blogger page</h1><p className="muted">Create a draft page through the Blogger API.</p></div><button className="btn btn-primary" disabled={busy||!t.trim()} onClick={save}>{busy?'Saving…':'Save page'}</button></div><div className="card"><div className="field"><label>Title</label><input value={t} onChange={e=>setT(e.target.value)}/></div><div className="field"><label>HTML content</label><textarea className="textarea" style={{minHeight:360}} value={c} onChange={e=>setC(e.target.value)}/></div></div></>}
