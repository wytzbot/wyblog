'use client';import {useEffect,useState} from 'react';import ModulePage from '@/components/ModulePage';export default function Page(){const [d,setD]=useState<any>();useEffect(()=>{fetch('/api/audit').then(r=>r.json()).then(setD)},[]);return <ModulePage title="SEO issues" description="Prioritize fixes by severity."><div className="card">{!d?<div className="skeleton" style={{height:220}}/>:d.issues.length?d.issues.map((i:any)=><div className="row" key={i.id+"-"+(i.postId||i.title||"")}><div><b>{i.message}</b><div className="kpi">{i.recommendation}</div></div><span className={i.status==='fail'?'status-bad':'status-warn'}>{i.severity}</span></div>):<div className="empty"><h3>Your site looks healthy.</h3></div>}</div></ModulePage>}
