'use client';import {useEffect,useState} from 'react';import Editor from '@/components/Editor';export default function Edit({params}:{params:Promise<{id:string}>}){const [d,setD]=useState<any>();useEffect(()=>{params.then(p=>fetch('/api/posts?id='+p.id).then(r=>r.json()).then(setD))},[params]);if(!d)return <div className="card skeleton" style={{height:500}}/>;return <Editor initial={d.post} blogId={d.post?.blogId}/>}
